
## 👑 **`sudo` - выполнение команд с правами суперпользователя**

### Основное использование:
```bash
sudo command          # выполнить команду с правами root
sudo -i               # войти в оболочку root
sudo su               # стать пользователем root
```

### Практические примеры:
```bash
# Установка программ
sudo apt update
sudo apt install nginx

# Редактирование системных файлов
sudo nano /etc/hosts
sudo vim /etc/ssh/sshd_config

# Управление службами
sudo systemctl start apache2
sudo systemctl restart docker

# Просмотр системных логов
sudo journalctl -xe
sudo tail -f /var/log/syslog
```

### Настройка sudo:
```bash
# Проверить права sudo
sudo whoami          # должен показать 'root'

# Посмотреть, кто может использовать sudo
sudo cat /etc/sudoers

# Добавить пользователя в группу sudo (если есть права)
sudo usermod -aG sudo username
```
##  **Проверка прав:**
```bash
# Посмотреть владельца и права
ls -l file.txt
# Вывод: -rw-r--r-- 1 user group 1234 Dec 10 file.txt
#         ↑ права    ↑ владелец ↑ группа
```

