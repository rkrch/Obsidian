### `rm` - удаление файлов
```bash
rm file.txt              # удалить файл
rm -r folder/            # удалить папку рекурсивно
rm -f file.txt           # принудительное удаление (без подтверждения)
# Удаленное нельзя восстановить!
```

