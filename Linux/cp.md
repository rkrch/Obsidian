### `cp` - копирование файлов
```bash
cp file1.txt file2.txt    # скопировать file1 в file2
cp -r folder1 folder2     # копировать папку рекурсивно
cp *.txt /backup/         # копировать все .txt файлы
```

