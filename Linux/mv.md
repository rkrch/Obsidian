### `mv` - перемещение/переименование
```bash
mv oldname.txt newname.txt    # переименовать файл
mv file.txt /new/folder/      # переместить файл
mv *.jpg /photos/            # переместить все jpg файлы
```
