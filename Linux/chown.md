

## 👤 **`chown` - изменение владельца файлов/папок**

### Синтаксис:
```bash
chown владелец файл
chown владелец:группа файл
```

### Практические примеры:
```bash
# Сменить владельца файла
sudo chown username file.txt

# Сменить владельца и группу
sudo chown username:group file.txt

# Рекурсивно для папки и всего содержимого
sudo chown -R username:group /path/to/folder/

# Сменить только группу
sudo chown :www-data file.html
```

### Частые случаи использования:
```bash
# Веб-сервер (nginx/apache)
sudo chown -R www-data:www-data /var/www/html

# Docker (если были проблемы с правами)
sudo chown -R $USER:$USER /home/username/docker_data

# Исправление прав после копирования файлов
sudo chown -R $USER:$USER /copied/folder
```

## 🔧 **Комбинации команд**

### Типичные сценарии:
```bash
# Создать папку в системной директории
sudo mkdir /opt/myapp
sudo chown $USER:$USER /opt/myapp

# Исправить права после случайного запуска с sudo
sudo chown -R $USER:$USER accidentally_owned_by_root/

# Настроить веб-сервер
sudo chown -R www-data:www-data /var/www
sudo chmod -R 755 /var/www
```
